import React from "react";
import { Button } from "./ui/button";

const MainNav = () => {
  return (
    <Button
      variant="ghost"
      className="font-bold hover:text-light-blue hover:bg-white"
    >
      Giriş Yap
    </Button>
  );
};

export default MainNav;
